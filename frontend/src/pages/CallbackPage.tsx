import { Container, Typography } from '@mui/material'
import { FC } from 'react'

export const CallbackPage: FC = () => {
  return (
    <Container>
      <Typography>
        Callback
      </Typography>
    </Container>
  )
}
